cmake_minimum_required(VERSION 3.1)
add_executable(PMCExecutable PMCExecutable.cpp)
target_link_libraries(PMCExecutable PUBLIC multiStateCommon)
if(WIN32)
	set_target_properties(PMCExecutable PROPERTIES RUNTIME_OUTPUT_DIRECTORY ${CMAKE_BINARY_DIR} PDB_OUTPUT_DIRECTORY ${CMAKE_BINARY_DIR})
endif()
